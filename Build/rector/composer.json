{
	"name": "jweiland/infinitescrolling-rector",
	"description": "Isolated Rector toolchain for jweiland/infinitescrolling, kept separate from the extension's own composer.json to avoid dependency conflicts",
	"license": "GPL-2.0-or-later",
	"type": "project",
	"require": {
		"php": ">=8.2",
		"ssch/typo3-rector": "^3.15"
	},
	"config": {
		"bin-dir": ".Build/bin",
		"sort-packages": true,
		"vendor-dir": ".Build/vendor"
	}
}
